# @aegis/core

Pure domain logic, framework-agnostic and heavily unit-tested: risk scoring,
crosswalk resolution, RBAC decisions, lifecycle state machines. No I/O, no framework
imports — this is where correctness lives.

Grows with each feature (M4+). See `BUILD-LOG.md`.
