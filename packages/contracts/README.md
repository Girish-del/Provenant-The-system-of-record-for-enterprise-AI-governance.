# @aegis/contracts

ts-rest API contracts + Zod schemas. Single source of truth for the web <-> api
boundary, giving end-to-end type safety with REST semantics. Generated OpenAPI for
partners/public API.

Populated alongside the first API endpoints (M2+). See `BUILD-LOG.md`.
