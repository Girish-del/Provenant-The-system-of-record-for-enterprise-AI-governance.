# @aegis/content

Framework + control library as content-as-data: EU AI Act, NIST AI RMF (+ GenAI
profile), ISO/IEC 42001, with crosswalks and questionnaire definitions. Versioned
rows, updatable without a code deploy — this is a core moat (keeps pace with
regulation). This package holds the seed source; runtime content lives in the DB.

Seeded in component 3.3. See `BUILD-LOG.md`.
