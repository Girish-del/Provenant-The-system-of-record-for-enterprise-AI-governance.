# @aegis/ui

Shared React component library (shadcn/ui base + 21st.dev blocks) and the design
tokens from `DESIGN.md` (Lapis primary, Sand accent, Carmine = critical only;
Fraunces + Geist + Geist Mono). Storybook with documented states.

Built starting at the first UI component (M4+), after design sign-off. See `BUILD-LOG.md`.
