# @aegis/api

NestJS core API: registry, risk, controls, evidence, workflows, reports. RBAC +
multi-tenancy (Postgres RLS). Type-safe contracts via ts-rest (`packages/contracts`).

Scaffolded in component 2.x (auth + tenancy). See `BUILD-LOG.md`.
